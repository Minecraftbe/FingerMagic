[project]
name = "fingermagic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "opencv-python>=4.10.0",
    "numpy>=2.0.0",
]

[dependency-groups]
dev = [
    "pyright>=1.1.411",
]
